import React from "react"

const Button = ({textColor}) => {
    return (
            <button className => {`${textColor} btn`}>
               Learn more
            </button>
    );       
}

export default Button